# ML Demo Foundation

This directory is reserved for the future MLflow demo.

Planned structure:
- `train.py` for a simple logistics model training script
- `inference.py` for model loading and prediction helpers
- `artifacts/` for exported model metadata outside version control

Do not commit trained artifacts or MLflow local runs.
