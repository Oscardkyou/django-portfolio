# AI Foundation

This directory is reserved for the future portfolio chatbot integration.

Planned structure:
- `prompts/` for system prompts and templates
- `knowledge_base/` for project and experience context
- `clients/` for OpenAI or local-model adapters
- `schemas/` for request/response contracts
